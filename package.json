{
  "scripts": {
    "build": "echo 'not set up'",
    "develop": "echo 'not set up'",
    "serve": "python3 -m http.server 3000 --directory ./dist/",
    "preserve": "npm run build",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "0.8.5",
    "gh-pages": "^3.2.2",
    "prettier": "2.3.1"
  },
  "engines": {
    "node": ">=14.x"
  },
  "type": "module",
  "private": true
}
