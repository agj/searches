{
  "scripts": {
    "build": "parcel build source/html/index.html --public-url ./",
    "prebuild": "rm -rf dist",
    "develop": "parcel source/html/index.html",
    "serve": "python3 -m http.server 1234 --directory ./dist/",
    "preserve": "npm run build",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build",
    "test": "npx elm-test"
  },
  "engines": {
    "node": ">=14.x"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "0.8.5",
    "gh-pages": "^4.0.0",
    "parcel": "^2.8.2",
    "prettier": "2.8.1",
    "@parcel/transformer-elm": "^2.8.2",
    "elm-test": "^0.19.1-revision10"
  }
}
